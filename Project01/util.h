
#pragma once
#include <iostream>
#include <string>
using namespace std;
void init_random(int* arr, int size, int minVal, int maxVal);
string to_str(int* arr, int size);
int sum_min_max(int min, int max, int* array);
int find_min_index(int* array, int size);
int find_max_index(int* array, int size);
