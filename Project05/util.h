#pragma once
#include <iostream>
#include <string>
using namespace std;
void init_random(int* arr, int size, int minVal, int maxVal);
string to_str(int* arr, int size); void bubble_sort(int arr[], int n, int a, int b, char direction);